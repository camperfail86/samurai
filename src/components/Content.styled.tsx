import styled from "styled-components";
import {myTheme} from "../styles/Theme.styled";

export const Content = styled.h2`
  background-color: transparent;
  color: ${myTheme.color.defaultBlack};
  font-size: 16px;
  margin: 20px auto;
  width: 260px;
`
