import styled from "styled-components";
import {myTheme} from "../styles/Theme.styled";

export const Paragraph = styled.p`
  color: ${myTheme.color.gray};
  font-size: 12px;
  width: 260px;
  margin: 0 auto;
`